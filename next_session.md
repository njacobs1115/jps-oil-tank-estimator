# Next Session — jps-oil-tank-estimator

## Open items
1. **End-to-end test** — break Make intentionally, confirm funnel-error contact lands in GHL with all fields
2. **GHL workflow** — Norman to build trigger on `funnel-error` tag → SMS/email Norman with contact name + phone + price
3. **Internal links** — add links from relevant site pages pointing to /oil-tank-removal-cost
4. **Point ads to new URL** — once end-to-end test passes

## First action
Check if Norman built the GHL workflow for `funnel-error` tag. If yes → run end-to-end test.
